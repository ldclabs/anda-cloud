{
  "dependencies": {},
  "devDependencies": {},
  "name": "anda_cloud_website",
  "private": true,
  "scripts": {
    "build": "tsc && vite build",
    "format": "prettier --write \"src/**/*.{json,js,jsx,ts,tsx,css,scss,svelte}\"",
    "setup": "npm i && dfx deploy",
    "start": "vite dev --port 3000",
    "ncu": "npx npm-check-updates -u",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "type": "module",
  "version": "0.1.1"
}